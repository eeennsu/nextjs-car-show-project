import type { FC } from 'react';

type Props = {
    title: string;
}

const CustomFilter: FC<Props> = ({ title }) => {



    return (
        <div>
            CustomFilter
        </div>
    );
}

export default CustomFilter;